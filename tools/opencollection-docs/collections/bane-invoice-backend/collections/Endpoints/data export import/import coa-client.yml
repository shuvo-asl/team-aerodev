info:
  name: import coa/client
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/api/export-import/"
  body:
    type: multipart-form
    data:
      - name: file
        type: file
        value:
          - /home/devs/Downloads/data (5).xls
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
