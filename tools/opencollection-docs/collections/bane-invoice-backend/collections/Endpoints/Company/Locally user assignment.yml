info:
  name: Locally user assignment
  type: http
  seq: 9

http:
  method: POST
  url: "{{url}}/api/assign-to-companies/"
  body:
    type: json
    data: |-
      {
          "user": 33
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
