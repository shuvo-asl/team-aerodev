info:
  name: dispute row list
  type: http
  seq: 6

http:
  method: GET
  url: "{{url}}/api/dispute-flights/?dispute_log={{dispute_log_id}}"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

runtime:
  variables:
    - name: dispute_log_id
      value: "3"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
