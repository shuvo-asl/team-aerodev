info:
  name: dispute log download source
  type: http
  seq: 9

http:
  method: GET
  url: "{{url}}/api/flight-disputes/{{dispute_log_id}}/download-source/"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
