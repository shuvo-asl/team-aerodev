info:
  name: dispute log list
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/flight-disputes/"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
