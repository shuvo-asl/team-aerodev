info:
  name: dispute log delete
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{url}}/api/flight-disputes/{{dispute_log_id}}/delete/"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
