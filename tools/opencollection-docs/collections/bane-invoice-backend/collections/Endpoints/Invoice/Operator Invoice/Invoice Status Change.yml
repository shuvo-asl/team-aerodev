info:
  name: Invoice Status Change
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{url}}/api/invoice-status-change/2/"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  body:
    type: json
    data: |-
      {
          "status": "approve"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
