info:
  name: get reserved keywords
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/api/reserved_keywords/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
