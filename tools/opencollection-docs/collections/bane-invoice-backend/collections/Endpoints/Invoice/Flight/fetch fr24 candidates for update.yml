info:
  name: fetch fr24 candidates for update
  type: http
  seq: 21

http:
  method: GET
  url: "{{url}}/api/flight/19190/fr24-candidates/"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
