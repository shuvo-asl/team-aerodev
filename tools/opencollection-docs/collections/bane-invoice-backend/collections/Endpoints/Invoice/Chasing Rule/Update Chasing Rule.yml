info:
  name: Update Chasing Rule
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{url}}/api/chasing_rule/4/"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  body:
    type: json
    data: |-
      {
          "name": "rule 4 updated",
          "is_default": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
