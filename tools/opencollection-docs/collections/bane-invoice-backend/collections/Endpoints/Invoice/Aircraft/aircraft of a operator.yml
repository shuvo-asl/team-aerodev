info:
  name: aircraft of a operator
  type: http
  seq: 4

http:
  method: GET
  url: "{{url}}/api/aircraft/?short_code=SAA"
  params:
    - name: short_code
      value: SAA
      type: query
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
