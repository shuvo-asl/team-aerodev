info:
  name: aircraft without operator
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/api/bas/aircraft-without-operator/?page=1&limit=10"
  params:
    - name: page
      value: "1"
      type: query
    - name: limit
      value: "10"
      type: query
  body:
    type: text
    data: |-
      {
          "icao": 745794,
          "registration_number": "yueuh",
          "operator_id": 1,
          "aircraft_type_id": 1
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
