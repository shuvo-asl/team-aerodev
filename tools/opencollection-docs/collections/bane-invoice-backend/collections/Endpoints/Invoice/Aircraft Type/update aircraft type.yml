info:
  name: update aircraft type
  type: http
  seq: 1

http:
  method: PATCH
  url: "{{url}}/api/bas/aircraft-type/1184/"
  body:
    type: json
    data: |-
      {
          "mtow": 120,
          "name": "type1"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
