info:
  name: delete aircraft type
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{url}}/api/bas/aircraft-type/1/"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
