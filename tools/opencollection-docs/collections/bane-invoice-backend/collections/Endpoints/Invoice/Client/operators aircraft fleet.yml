info:
  name: operators aircraft fleet
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/api/fleet/operator/FAD/"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
