info:
  name: delete clients additional email
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{url}}/api/clients-additional-email/8/?client=40"
  params:
    - name: client
      value: "40"
      type: query
    - name: page
      value: "1"
      type: query
      disabled: true
    - name: limit
      value: "2"
      type: query
      disabled: true
  body:
    type: text
    data: |-
      {
          "client": 40, 
          "emails": ["client6@gmail.com"]
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
