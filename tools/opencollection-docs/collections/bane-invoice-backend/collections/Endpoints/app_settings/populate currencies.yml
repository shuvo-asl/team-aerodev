info:
  name: populate currencies
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{url}}/api/populate-currencies/"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
