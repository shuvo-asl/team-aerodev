info:
  name: Delete API Token
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{url}}/api-token/2"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: Delete API Token eg
    request:
      url: "{{url}}/api-token/2"
      method: DELETE
    response:
      body:
        type: text
        data: ""
