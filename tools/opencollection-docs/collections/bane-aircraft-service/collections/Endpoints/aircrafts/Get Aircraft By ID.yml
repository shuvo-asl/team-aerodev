info:
  name: Get Aircraft By ID
  type: http
  seq: 7

http:
  method: GET
  url: "{{url}}/aircrafts?id=176"
  params:
    - name: id
      value: "176"
      type: query
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
