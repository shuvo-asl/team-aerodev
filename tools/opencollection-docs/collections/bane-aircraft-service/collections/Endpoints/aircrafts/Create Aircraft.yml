info:
  name: Create Aircraft
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/aircrafts"
  body:
    type: json
    data: |-
      {
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
