info:
  name: air craft types
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/aircraft-types?all=true"
  params:
    - name: all
      value: "true"
      type: query
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
