info:
  name: Get Aircrafts
  type: http
  seq: 4

http:
  method: GET
  url: "{{url}}/aircrafts"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
