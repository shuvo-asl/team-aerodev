info:
  name: aircrafts by icao24
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/aircraft/icao24/FCVBHJ"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
