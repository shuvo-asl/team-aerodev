info:
  name: Get Locations
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/locations"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: Get Locations eg
    request:
      url: "{{url}}/locations"
      method: GET
    response:
      body:
        type: text
        data: ""
