info:
  name: Get Location By ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/locations/100000"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
