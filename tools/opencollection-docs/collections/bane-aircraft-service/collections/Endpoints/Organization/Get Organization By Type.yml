info:
  name: Get Organization By Type
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/organizations/OSP Agent"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
