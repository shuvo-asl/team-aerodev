info:
  name: Get Organizations
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/organizations"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
