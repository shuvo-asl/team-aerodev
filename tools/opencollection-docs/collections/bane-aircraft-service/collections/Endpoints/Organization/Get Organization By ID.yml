info:
  name: Get Organization By ID
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/organizations/200"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
