info:
  name: create aircraft type
  type: grpc
  seq: 8

grpc:
  url: "{{gRPC_url}}"
  method: /bas.BAS/CreateAircraftType
  methodType: unary
  protoFilePath: ../../../rpc/bas.proto
  metadata:
    - name: "{{auth_param_name}}"
      value: "{{token}}"
  message: |-
    {
        "name": "B-7389",
        "mtow": 100,
        "mtow_unit": "ton"
    }
  auth: inherit
