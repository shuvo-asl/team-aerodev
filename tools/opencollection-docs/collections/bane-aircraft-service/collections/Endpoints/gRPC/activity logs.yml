info:
  name: activity logs
  type: grpc
  seq: 1

grpc:
  url: "{{gRPC_url}}"
  method: /bas.BAS/GetActivityLogs
  methodType: unary
  protoFilePath: ../../../rpc/bas.proto
  metadata:
    - name: "{{auth_param_name}}"
      value: "{{token}}"
  message: |-
    {
        "model": "AircraftType",
        "object": 1182
    }
  auth: inherit
