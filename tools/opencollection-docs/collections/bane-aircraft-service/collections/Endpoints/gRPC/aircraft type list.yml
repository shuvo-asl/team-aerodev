info:
  name: aircraft type list
  type: grpc
  seq: 3

grpc:
  url: "{{gRPC_url}}"
  method: /bas.BAS/GetAircraftTypes
  methodType: unary
  protoFilePath: ../../../rpc/bas.proto
  metadata:
    - name: "{{auth_param_name}}"
      value: "{{token}}"
  message: "{}"
  auth: inherit
