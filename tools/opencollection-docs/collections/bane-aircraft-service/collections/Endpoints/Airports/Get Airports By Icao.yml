info:
  name: Get Airports By Icao
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/airports?icao=VGHS"
  params:
    - name: icao
      value: VGHS
      type: query
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
