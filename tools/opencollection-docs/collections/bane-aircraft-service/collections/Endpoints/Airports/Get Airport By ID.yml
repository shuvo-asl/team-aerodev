info:
  name: Get Airport By ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/airports/45615521"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
