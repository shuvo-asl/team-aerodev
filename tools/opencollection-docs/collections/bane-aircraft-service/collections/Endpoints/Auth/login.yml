info:
  name: login
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/users"
  body:
    type: json
    data: |-
      {
          "user_name": "admin",
          "password": "admin"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
