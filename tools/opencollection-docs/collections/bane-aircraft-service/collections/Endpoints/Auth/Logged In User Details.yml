info:
  name: Logged In User Details
  type: http
  seq: 5

http:
  method: GET
  url: "{{url}}/users"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
